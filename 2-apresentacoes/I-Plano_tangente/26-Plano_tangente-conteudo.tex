%------------------------------------------------------------------------------%

\usepackage{calculo_varias_variaveis-1}

\title{Planos Tangentes}

%------------------------------------------------------------------------------%
\begin{document}

\addtitle

%------------------------------------------------------------------------------%
\section{Plano Tangente à Superfície de Nível}

%------------------------------------------------------------------------------%
\begin{frame}{Justificativa}
  Primeiro vamos verificar que o gradiente é normal a superfície de nível

  \pause
  Considere uma função $f(x, y)$ diferenciável e sua superfície de nível
  \[
    f(x, y, z)  = c
  \]
  \pause
  Assuma que a curva paramétrica
  \[
    r(t)
      = g(t)\veci + h(t)\vecj + k(t)\veck
      = \left(\begin{array}{c}
          g(t) \\ h(t) \\ k(t)
        \end{array}\right)
  \]
  \pause
  está contida na superfície de nível $c$ da função $f$
\end{frame}

%------------------------------------------------------------------------------%
\begin{frame}{Justificativa}
\begin{columns}
\column{0.5\linewidth}
  \begin{align*}
     \onslide<+->{f\big(g(t), h(t), k(t)\big) & = c \\[5mm]}
     \onslide<+->{\frac{d}{dt}f\big(g(t), h(t), k(t)\big) & = \frac{dc}{dt} \\[5mm]}
     \onslide<+->{\D{f}{x}\frac{dg}{dt} + \D{f}{y}\frac{dh}{dt} + \D{f}{z}\frac{dk}{dt} & = 0}
  \end{align*}
\column{0.5\linewidth}
  \begin{align*}
    \onslide<+->{\begin{pmatrix}
        \D{f}{x} \\[3mm]
        \D{f}{y} \\[3mm]
        \D{f}{z}
      \end{pmatrix}
      \cdot
      \begin{pmatrix}
        \frac{dg}{dt} \\[3mm]
        \frac{dh}{dt} \\[3mm]
        \frac{dk}{dt}
      \end{pmatrix}
      & = 0 \\[2mm]}
    \onslide<+->{\nabla f \cdot \frac{dr}{dt} & = 0}
  \end{align*}
\end{columns}
\end{frame}

%------------------------------------------------------------------------------%
\begin{frame}{Justificativa}
  Se tomarmos todas as curvas suaves que passam por um ponto \((x_0, y_0)\)

  \pause
  \vspace{\baselineskip}
  Seus vetores tangentes são todos ortogonais ao vetor gradiente no ponto

  \pause
  \vspace{\baselineskip}
  Todos os vetores tangentes estão no plano tangente a superfície
\end{frame}

%------------------------------------------------------------------------------%
\begin{frame}{Definição}
  O Plano Tangente ao ponto \((x_0, y_0, z_0)\)
  \pause
  na superfície de nível
  \(f(x, y, z) = c\)

  \pause
  de uma função diferenciável $f$
  \pause
  é o plano passando pelo ponto \((x_0, y_0, z_0)\)

  \pause
  e normal a \(\nabla f (x_0, y_0, z_0)\)

  \pause
  \vspace{\baselineskip}
  A Reta Normal da superfície no ponto é a reta passando pelo ponto

  \pause
  e paralela
  a \(\nabla f (x_0, y_0, z_0)\)
\end{frame}

%------------------------------------------------------------------------------%
\begin{frame}{Equação do Plano Tangente}
  \[
    \emph{\nabla f(x_0, y_0, z_0)} \cdot (X-X_0) = 0
  \]
  \pause
  \[
    \emph{\nabla f(x_0, y_0, z_0)} \cdot \left(
        \begin{array}{c}
          x - x_0 \\
          y - y_0 \\
          z - z_0
        \end{array}
      \right) = 0
  \]

  \pause
  \[
    \emph{f_x(x_0, y_0, z_0)} (x-x_0) +
    \emph{f_y(x_0, y_0, z_0)} (y-y_0) +
    \emph{f_z(x_0, y_0, z_0)} (z-z_0) = 0
  \]
\end{frame}

%------------------------------------------------------------------------------%
\begin{frame}{Equação da Reta Normal}
  \[
    X = X_0 + t\,\emph{\nabla f(x_0, y_0, z_0)}
    \qquad\qquad
    t \in \R
  \]

  \pause
  \[
    \begin{pmatrix} x \\ y \\ z \end{pmatrix}
    = \begin{pmatrix} x_0\\ y_0 \\ z_0 \end{pmatrix}
    + t \begin{pmatrix}
      \emph{f_x(x_0, y_0, z_0)} \\
      \emph{f_y(x_0, y_0, z_0)} \\
      \emph{f_z(x_0, y_0, z_0)}
    \end{pmatrix}
  \]

  \pause
  \[
    \begin{cases}
      x = x_0 + t\,\emph{f_x(x_0, y_0, z_0)} \\
      y = y_0 + t\,\emph{f_y(x_0, y_0, z_0)} \\
      z = z_0 + t\,\emph{f_z(x_0, y_0, z_0)}
    \end{cases}
  \]
\end{frame}

%------------------------------------------------------------------------------%
\addtocounter{exemplo}{1}
\begin{frame}{Exemplo \theexemplo}
  Encontre o plano tangente e a reta normal da superfície
  \[
    f(x, y, z) = x^2 + y^2 + z - 9 = 0
  \]
  no ponto \((1, 2, 4)\)
\end{frame}

%------------------------------------------------------------------------------%
\begin{frame}{Exemplo \theexemplo}
\begin{columns}
\column{0.45\linewidth}
  \begin{align*}
    \onslide<+->{\D{f}{x} & = \D{}{x} \left(x^2 + y^2 + z - 9\right) = 2x \\[5mm]}
    \onslide<+->{\D{f}{y} & = \D{}{y} \left(x^2 + y^2 + z - 9\right) = 2y \\[5mm]}
    \onslide<+->{\D{f}{z} & = \D{}{z} \left(x^2 + y^2 + z - 9\right) = 1}
  \end{align*}
\column{0.55\linewidth}
  \[
    \onslide<+->{\nabla f}
    \onslide<+->{= 2x\veci + 2y\vecj + \veck}
    \onslide<+->{= \left(\begin{array}{c}
        2x \\ 2y \\ 1
      \end{array}\right)}
  \]
  \[
    \onslide<+->{\nabla f(1, 2, 4)}
    \onslide<+->{= \left(2x\veci + 2y\vecj + \veck\right)\evalat{(1, 2, 4)}{}}
  \]
  \[
    \hphantom{\nabla f(1, 2, 4)}
    \onslide<+->{= 2\veci + 4\vecj + \veck}
    \onslide<+->{= \left(\begin{array}{c}
         2 \\ 4 \\ 1
      \end{array}\right)}
  \]
\end{columns}
\end{frame}

%------------------------------------------------------------------------------%
\begin{frame}{Exemplo \theexemplo}
  Plano tangente
  \[
    \pause
    \nabla f(x_0, y_0, z_0) \cdot  \left(
    \begin{array}{c}
      x - x_0 \\
      y - y_0 \\
      z - z_0
    \end{array}
    \right) = 0
  \]

  \[
    \pause
    f_x(x_0, y_0, z_0) (x-x_0) +
    f_y(x_0, y_0, z_0) (y-y_0) +
    f_z(x_0, y_0, z_0) (z-z_0) = 0
  \]

  \[
    \pause
    f_x(1, 2, 4) (x-1) +
    f_y(1, 2, 4) (y-2) +
    f_z(1, 2, 4) (z-4) = 0
  \]

  \[
    \pause
    2 (x-1) + 4 (y-2) + (z-4) = 0
  \]

  \[
    \pause
    2x + 4y + z = 14
  \]
\end{frame}

%------------------------------------------------------------------------------%
\begin{frame}{Exemplo \theexemplo}
  Reta normal
  \pause
  \[
    X = X_0 + t\,\nabla f(x_0, y_0, z_0)
    \qquad\qquad
    t \in \R
  \]

  \pause
  \[
    \begin{cases}
      x = x_0 + t\,f_x(x_0, y_0, z_0) \\
      y = y_0 + t\,f_y(x_0, y_0, z_0) \\
      z = z_0 + t\,f_z(x_0, y_0, z_0)
    \end{cases}
  \]

  \pause
  \[
    \begin{cases}
      x = 1 + 2t \\
      y = 2 + 4t \\
      z = 4 + t
    \end{cases}
  \]
\end{frame}

%------------------------------------------------------------------------------%
\section{Plano Tangente ao Gráfico de uma Função}

%------------------------------------------------------------------------------%
\begin{frame}{Justificativa}
  Queremos o Plano Tangente a superfície suave \(z=f(x, y)\) \\
  em um ponto \((x_0, y_0, z_0)\)
  onde \(z_0 = f(x_0, y_0)\)

  \pause
  \vspace{\baselineskip}
  Podemos escrever \(z=f(x, y)\) como \(f(x, y) - z = 0\)

  \pause
  \vspace{\baselineskip}
  Queremos o plano tangente a curva de nível zero da função
  \[
    F(x, y, z) = f(x, y) - z = 0
  \]
\end{frame}

%------------------------------------------------------------------------------%
\begin{frame}{Justificativa}
  Derivadas de $F$
  \begin{align*}
    \onslide<+->{F_x = \D{}{x}\left(f(x, y) - z\right) = f_x - 0 = f_x \\[5mm]}
    \onslide<+->{F_y = \D{}{y}\left(f(x, y) - z\right) = f_y - 0 = f_y \\[5mm]}
    \onslide<+->{F_z = \D{}{z}\left(f(x, y) - z\right) = 0 - 1 = -1}
  \end{align*}
\end{frame}

%------------------------------------------------------------------------------%
\begin{frame}{Justificativa}
  Equação do Plano Tangente ao gráfico da função $f(x, y)$
  \[
    \emph{\nabla F(x_0, y_0, z_0)} \cdot
    \begin{pmatrix}
      x - x_0 \\
      y - y_0 \\
      z - z_0
    \end{pmatrix} = 0
  \]

  \pause
  \[
    \emph{F_x(x_0, y_0, z_0)} (x-x_0) +
    \emph{F_y(x_0, y_0, z_0)} (y-y_0) +
    (z-z_0) = 0
  \]

  \pause
  \[
    {\color{magenta} f_x(x_0, y_0)} (x-x_0) +
    {\color{magenta} f_y(x_0, y_0)} (y-y_0) -
    (z-z_0) = 0
  \]
\end{frame}

%------------------------------------------------------------------------------%
\begin{frame}{Plano Tangente a superfície $z=f(x, y)$}
  O plano tangente à superfície \(z=f(x, y)\) \\
  de uma função diferenciável $f$
  no ponto \(\left(x_0, y_0, f(x_0, y_0)\right)\) é
  \[
    f_x(x_0, y_0) (x-x_0) +
    f_y(x_0, y_0) (y-y_0) -
    (z-z_0) = 0
  \]
\end{frame}

%------------------------------------------------------------------------------%
\addtocounter{exemplo}{1}
\begin{frame}{Exemplo \theexemplo}
  Encontre o plano tangente à superfície
  \[
    z = x\cos(y) - ye^x
  \]
  no ponto \((x_0, y_0, z_0) = (0, 0, 0)\)
\end{frame}

%------------------------------------------------------------------------------%
\begin{frame}{Exemplo \theexemplo}
    Essa superfície corresponde ao gráfico da função \(f(x, y) = x\cos(y) - ye^x\)

  \pause
  \vspace{\baselineskip}
  Conferindo que o ponto $(0, 0, 0)$ está na superfície
  \[
    \pause
    f(0, 0)
    \pause
    \;=\; \left(x\cos(y) - ye^x\right)\evalat{(0, 0)}{}
    \pause
    \;=\; 0\times\cos(0) - 0\times e^0
    \pause
    \;=\; 0
  \]
\end{frame}

\begin{frame}{Exemplo \theexemplo}
  \onslide<+->{Derivadas parciais de $f(x, y)$}
  \begin{align*}
    \onslide<+->{\D{f}{x}}
    \onslide<+->{& = \D{}{x} \left(x\cos(y) - ye^x\right)}
    \onslide<+->{  = \cos(y) - ye^x \\[3mm]}
    \onslide<+->{\D{f}{y}}
    \onslide<+->{& = \D{}{y} \left(x\cos(y) - ye^x\right)}
    \onslide<+->{  = -x\sin(y) - e^x}
  \end{align*}
  \begin{align*}
    \onslide<+->{\D{f}{x}(0, 0)}
    \onslide<+->{& = \left(  \cos(y) - ye^x\right) \evalat{(0, 0)}{}}
    \onslide<+->{  = 1 \\[3mm]}
    \onslide<+->{\D{f}{y}(0, 0)}
    \onslide<+->{& = \left(-x\sin(y) - e^x \right) \evalat{(0, 0)}{}}
    \onslide<+->{  = -1}
  \end{align*}
\end{frame}

\begin{frame}{Exemplo \theexemplo}
  Plano tangente
  \[
    \emph{f_x(x_0, y_0)} (x-x_0) +
    \emph{f_y(x_0, y_0)} (y-y_0) -
    (z-z_0) = 0
  \]

  \pause
  \[
    \emph{f_x(0, 0)} (x-0) +
    \emph{f_y(0, 0)} (y-0) -
    (z-0) = 0
  \]

  \pause
  \[
    \emph{1} (x-0) -
    \emph{1} (y-0) -
      (z-0) = 0
  \]

  \pause
  \[
    x - y - z = 0
  \]
\end{frame}

%------------------------------------------------------------------------------%
\section{Lista Mínima}

%------------------------------------------------------------------------------%
\begin{frame}{Lista Mínima}
  Cálculo Vol. 2 do Thomas 12\textsuperscript{a} ed. -- Seção 14.6
  \begin{enumerate}
    \item Estudar o texto da seção
    \item Resolver os exercícios: 2, 4, 6, 8
  \end{enumerate}

  \vfill
  Atenção: A prova é baseada no livro, não nas apresentações
\end{frame}

%------------------------------------------------------------------------------%
\end{document}
%------------------------------------------------------------------------------%
