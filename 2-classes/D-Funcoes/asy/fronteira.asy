//-----------------------------------------------------------------------------

import "../../0-common/asy/utils.ah" as utils;

size(70mm);

real R = 1;               

draw(box((-1.2,-1.2), (1.2,1.2)), invisible);

filldraw(circle((0,0), R), gray(0.9), pens[0]);
label("$R$", (-0.5, 0.7));

//-----------------------------------------------------------------------------
